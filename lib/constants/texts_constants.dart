import 'package:flutter/material.dart';

import 'textyle_constants.dart';

const kShoppingText = Text(
  "Shopping",
  style: kShoppingStyle,
);
const kError = Text("error");
const kAdd = Text("Add");
const kAdded = Text("Added");
const kCart = Text(
  "Cart",
  style: kCartStyle,
);
const kAddItemsFirst = "Add some items first";
const kBUY = Text("BUY");
const kShopping = Text(
  "Shopping",
  style: kCartStyle,
);
const kOrders = Text(
  "Orders",
  style: kCartStyle,
);

const kNoPurchaseHistoryAvailable = "No purchase history available";
const KNoHistory = Text(
  kNoPurchaseHistoryAvailable,
  style: TextStyle(fontSize: 20),
);
